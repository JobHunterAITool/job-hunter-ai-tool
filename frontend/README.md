# Job Hunter AI Tool -- Frontend

This repository contains the React + Vite frontend for the **Job Hunter
AI Tool** capstone project.

The frontend allows users to enter job search information, upload a
resume, send requests to the backend API, and view ranked job matches
returned by the system.

The goal of this interface is to provide a simple way for users to
submit job search information and quickly see relevant job results
generated by the backend machine learning service.

# Overview

The frontend collects user input through a search form and sends that
information to the backend API. The backend processes the request and
returns ranked job matches, which are then displayed in the interface.

Users may also upload a resume file (PDF or DOCX). The resume is sent to
the backend where text is extracted and used to assist with job
matching.

The application was created using:

-   **React** for the component-based interface
-   **Vite** for fast development builds and hot module replacement
-   **Axios** for communication between the frontend and backend API

# Project Structure

The frontend directory is organized as follows:

```text
frontend/
├── public/
├── src/
│   ├── assets/
│   ├── components/
│       ├── JobResults.jsx
│       ├── ResumeUpload.jsx
│       ├── SearchForm.jsx
│   ├── services
│       ├── api.js
│   ├── App.css
│   ├── App.jsx
│   ├── index.css
│   └── main.jsx
├── eslint.config.js
├── index.html
├── package-lock.json
├── package.json
├── README.md
└── vite.config.js


# Prerequisites

Before running the frontend, make sure the following are installed:

-   Node.js
-   npm

You can verify this with:

node -v\
npm -v


# Installation

Install the project dependencies:

npm install

This will install all required packages including:

-   React
-   Vite
-   Axios


# Running the Development Server

Start the development server with:

npm run dev

After starting the server, Vite will provide a local address, typically:

http://localhost:5173

The development server supports hot module replacement, meaning the page
will update automatically when changes are made to the code.

# Backend API Connection

The frontend communicates with the backend using **Axios**.

API requests are defined in:

src/services/api.js

The frontend sends requests to the backend for:

-   Job search requests (`POST /search`)
-   Resume uploads (`POST /upload-resume`)

The backend URL can be configured using an environment variable:

VITE_API_BASE_URL=http://127.0.0.1:8000

If no environment variable is set, the frontend defaults to:

http://127.0.0.1:8000


# Resume Upload Feature

Users may upload a resume by dragging and dropping a file into the
upload area or selecting a file manually.

Supported formats:

-   PDF
-   DOCX

The resume file is sent to the backend where a short text preview is
extracted and returned to the frontend.


# Linting

To check the code using ESLint:

npm run lint


# Available Scripts

Common scripts used in this project:

npm run dev \# start development server\
npm run build \# create production build\
npm run preview \# preview the production build locally\
npm run lint \# run ESLint checks


# Building for Production

To generate a production build:

npm run build

To preview the production build locally:

npm run preview

## Frontend Testing

The frontend includes automated tests for key UI components using **Vitest** and **React Testing Library**. These tools allow the application to simulate user interactions and verify that components behave correctly.

The current test suite focuses on the `ResumeUpload` component, which allows users to upload a PDF or DOCX resume and preview extracted text returned from the backend.

### Test Coverage

The `ResumeUpload` component tests verify that:

- The component renders correctly in the interface
- An error message appears when attempting to upload without selecting a file
- Valid **PDF** files are accepted and displayed
- Valid **DOCX** files are accepted and displayed
- Invalid file types are rejected
- Successful uploads display the returned resume preview
- Upload failures display an appropriate error message

These tests simulate file uploads and API responses using mocked service calls, allowing the component behavior to be validated without requiring the backend server to be running.

### Running Tests

Navigate to the frontend directory and run:

```bash
cd frontend
npm install
npm run test


# Summary

This frontend provides the user interface for the **Job Hunter AI
Tool**. It allows users to submit job search information, upload a
resume, and view ranked job results returned by the backend service.

The project uses React, Vite, and Axios to create a fast and
maintainable frontend for interacting with the backend job matching
system.
